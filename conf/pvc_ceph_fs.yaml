apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  namespace: namespace-test
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: ocs-storagecluster-cephfs
